spring:
  application:
    name: admin-server

---
profiles: local
server:
  port: 30001
kafka:
  clusters:
    - clusterName: kafka-test
      clusterId:
      bootstrapServers:
        - localhost:9092